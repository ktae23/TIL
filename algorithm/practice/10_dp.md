# 10. 동적 프로그래밍 (Dynamic Programming)

> 관련 코드: `section3_essential1/dp/DPBasic.java`

---

## 1. DP란?

복잡한 문제를 **작은 부분 문제로 나누어 해결**하고, 그 결과를 **저장하여 재사용**하는 알고리즘 설계 기법입니다.

### 핵심 아이디어
- 이미 계산한 값을 다시 계산하지 않음 (**메모이제이션**)
- 작은 문제의 해를 이용해 큰 문제의 해를 구함

---

## 2. DP 적용 조건

### 1) 최적 부분 구조 (Optimal Substructure)
큰 문제의 최적해가 **작은 문제의 최적해로 구성**됨

### 2) 중복되는 부분 문제 (Overlapping Subproblems)
같은 부분 문제가 **여러 번 반복**됨

> 분할 정복과의 차이: 분할 정복은 부분 문제가 중복되지 않음

---

## 3. DP 구현 방식

### Top-Down (메모이제이션)
- **재귀 + 캐싱**
- 필요한 부분 문제만 계산
- 직관적이지만 스택 오버플로우 주의

```java
static long[] memo = new long[100];

public static long fibTopDown(int n) {
    if (n <= 1) return n;
    if (memo[n] != 0) return memo[n];  // 이미 계산했으면 반환
    return memo[n] = fibTopDown(n - 1) + fibTopDown(n - 2);
}
```

### Bottom-Up (타뷸레이션)
- **반복문**으로 작은 문제부터 해결
- 모든 부분 문제를 계산
- 메모리 최적화 가능

```java
public static long fibBottomUp(int n) {
    if (n <= 1) return n;

    long[] dp = new long[n + 1];
    dp[0] = 0;
    dp[1] = 1;

    for (int i = 2; i <= n; i++) {
        dp[i] = dp[i - 1] + dp[i - 2];
    }
    return dp[n];
}
```

---

## 4. DP 문제 접근법

### 1) 상태 정의
`dp[i]`가 **무엇을 의미하는지** 정의

### 2) 점화식 수립
`dp[i]`를 **이전 상태들로 표현**

### 3) 초기값 설정
기저 조건 (Base Case)

### 4) 계산 순서 결정
의존 관계에 따라 계산

---

## 5. 대표적인 DP 문제

### 피보나치 수열
```
점화식: F(n) = F(n-1) + F(n-2)
초기값: F(0) = 0, F(1) = 1
```

### 계단 오르기
```java
// 한 번에 1계단 또는 2계단
// 점화식: dp[i] = dp[i-1] + dp[i-2]
public static int climbStairs(int n) {
    if (n <= 2) return n;
    int[] dp = new int[n + 1];
    dp[1] = 1;
    dp[2] = 2;
    for (int i = 3; i <= n; i++) {
        dp[i] = dp[i - 1] + dp[i - 2];
    }
    return dp[n];
}
```

### 동전 교환
```java
// 점화식: dp[i] = min(dp[i], dp[i - coin] + 1)
public static int coinChange(int[] coins, int amount) {
    int[] dp = new int[amount + 1];
    int INF = amount + 1;
    Arrays.fill(dp, INF);
    dp[0] = 0;

    for (int i = 1; i <= amount; i++) {
        for (int coin : coins) {
            if (i >= coin && dp[i - coin] != INF) {
                dp[i] = Math.min(dp[i], dp[i - coin] + 1);
            }
        }
    }
    return dp[amount] == INF ? -1 : dp[amount];
}
```

### 격자 경로 수
```java
// 점화식: dp[i][j] = dp[i-1][j] + dp[i][j-1]
public static int uniquePaths(int m, int n) {
    int[][] dp = new int[m][n];

    for (int i = 0; i < m; i++) dp[i][0] = 1;
    for (int j = 0; j < n; j++) dp[0][j] = 1;

    for (int i = 1; i < m; i++) {
        for (int j = 1; j < n; j++) {
            dp[i][j] = dp[i - 1][j] + dp[i][j - 1];
        }
    }
    return dp[m - 1][n - 1];
}
```

### 최대 부분 배열 합 (Kadane's Algorithm)
```java
// 점화식: dp[i] = max(dp[i-1] + arr[i], arr[i])
public static int maxSubArray(int[] arr) {
    int maxSum = arr[0];
    int currentSum = arr[0];

    for (int i = 1; i < arr.length; i++) {
        currentSum = Math.max(currentSum + arr[i], arr[i]);
        maxSum = Math.max(maxSum, currentSum);
    }
    return maxSum;
}
```

---

## 6. 공간 최적화

```java
// 피보나치: O(n) → O(1)
public static long fibOptimized(int n) {
    if (n <= 1) return n;

    long prev2 = 0, prev1 = 1;
    for (int i = 2; i <= n; i++) {
        long current = prev1 + prev2;
        prev2 = prev1;
        prev1 = current;
    }
    return prev1;
}
```

---

## 핵심 정리

1. **상태 정의**가 가장 중요
2. **점화식**을 찾아라
3. Top-Down vs Bottom-Up 선택
4. 공간 최적화 가능한지 확인
